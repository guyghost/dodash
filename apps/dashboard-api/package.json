{
  "name": "@dodash/dashboard-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev --port 8789",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260817.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  }
}
