{
  "name": "dodash",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "backtest": "pnpm --filter @dodash/backtest run:coinbase",
    "build": "turbo run build",
    "check": "turbo run check",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.10.10",
    "typescript": "^7.0.2"
  }
}
